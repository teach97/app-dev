# FastAPI 문서화 바이브코딩 프롬프트

대상 프로젝트:

- `C:\Users\Playdata\Desktop\study\app-dev\main.py`
- `C:\Users\Playdata\Desktop\study\app-dev\schemas.py`

## 사용 순서

아래 프롬프트를 한 번에 실행하지 않고 다음 순서대로 사용합니다.

1. 문서화 누락 점검
2. `main.py` 문서화
3. `schemas.py` 문서화
4. 최종 누락 점검 및 검증

---

## 1. 문서화 누락 점검

```text
현재 프로젝트의 FastAPI 문서화 상태를 점검해 주세요.

대상 파일:
- C:\Users\Playdata\Desktop\study\app-dev\main.py
- C:\Users\Playdata\Desktop\study\app-dev\schemas.py

코드를 수정하지 말고 아래 항목만 표로 정리해 주세요.

1. tags가 없는 엔드포인트
2. summary가 없는 엔드포인트
3. 독스트링이 없는 엔드포인트
4. 코드에 실제로 존재하는 raise HTTPException 상태 코드
5. responses에 문서화되지 않은 실제 오류 상태 코드
6. responses에는 있지만 실제로 raise하지 않는 상태 코드
7. description이 없는 Pydantic 모델 필드
8. examples가 없는 주요 필드
9. 기본값이 있는 선택 필드인데 Field(default=...)가 누락된 필드

반드시 실제 코드에 존재하는 내용만 근거로 판단하세요.
없는 엔드포인트나 오류를 추측해서 추가하지 마세요.
각 항목에는 파일명, 함수명 또는 필드명과 코드 근거를 함께 작성하세요.
```

---

## 2. main.py 문서화

```text
현재 프로젝트의 기능과 로직은 변경하지 않고 FastAPI 문서화만 개선해 주세요.

대상 파일:
- C:\Users\Playdata\Desktop\study\app-dev\main.py

목표:
Swagger UI(/docs), ReDoc(/redoc), OpenAPI 원본(/openapi.json)에서 API를 이해하고 테스트하기 쉽게 만듭니다.

태그는 아래 4개만 사용하세요. 새로운 태그를 만들지 마세요.

- 시스템: /, /health, /info
- 도서: /books 관련 엔드포인트
- 외부 연동: /weather*, /books/external*
- 학습용: /slow-async, /slow-block

실제 파일에 존재하는 엔드포인트에만 태그를 추가하세요.

작업 내용:

1. FastAPI 앱에 title, description, version, contact를 추가하세요.
2. openapi_tags로 태그 설명과 표시 순서를 지정하세요.
3. 각 엔드포인트에 tags를 추가하세요.
4. summary는 한국어 명사형으로 작성하세요.
   예: "도서 등록", "도서 단건 조회", "Google Books 검색"
5. 함수 정의 바로 다음 줄에 독스트링을 추가하세요.
6. 독스트링에는 실제 함수 파라미터만 아래 형식으로 설명하세요.
   - **파라미터명**: 설명
7. response_description을 추가해 성공 응답을 설명하세요.
8. 코드에 실제로 존재하는 raise HTTPException 상태 코드만 responses에 추가하세요.
9. 200과 422는 FastAPI가 자동 생성하므로 responses에 추가하지 마세요.
10. responses의 description은 실제 오류 내용을 기준으로 작성하세요.
11. 문서에 없는 기능, 파라미터, 오류를 추측해서 작성하지 마세요.

절대 하지 말 것:

- 함수 본문 수정
- 비즈니스 로직 수정
- API 경로 수정
- HTTP 메서드 수정
- 엔드포인트 선언 순서 변경
- 외부 API 호출 방식 변경
- 응답 데이터 구조 변경
- 기존 검증 조건 변경
- 없는 기능이나 오류를 임의로 추가
- /books/{book_id}보다 고정 경로를 아래로 이동

작업 후 다음을 확인하세요.

1. Python 문법 오류가 없는지 확인
2. 서버가 정상적으로 실행되는지 확인
3. /docs에 default 태그 그룹이 남아 있지 않은지 확인
4. /openapi.json에 responses 상태 코드가 실제로 반영됐는지 확인
5. 기존 API 기능이 변경되지 않았는지 확인

전체 파일을 불필요하게 다시 출력하지 말고, 수정된 코드와 변경 요약을 보여 주세요.
```

---

## 3. schemas.py 문서화

```text
현재 프로젝트의 Pydantic 모델에 문서화 정보만 추가해 주세요.

대상 파일:
- C:\Users\Playdata\Desktop\study\app-dev\schemas.py

작업 내용:

1. Pydantic 모델 필드에 한국어 description을 추가하세요.
2. 주요 필드에 실제 사용 가능한 examples를 추가하세요.
3. 기존 min_length, max_length, ge, le 등 검증 조건은 그대로 유지하세요.
4. 기본값이 있는 선택 필드를 Field로 감쌀 때는 반드시 default=를 명시하세요.
   예: publisher: Publisher | None = Field(default=None, description="출판사 정보")
5. BookCreate에는 model_config의 json_schema_extra로 실제 요청 예시를 추가하세요.
6. BookResponse의 id 필드에는 서버 발급 번호 설명과 예시를 추가하세요.
7. WeatherResponse, ExternalBook 등 실제 사용되는 응답 모델에도 필드 설명과 예시를 추가하세요.
8. list 타입의 examples는 실제 자료형에 맞게 작성하세요.
   예: examples=[["python", "web"]]

절대 하지 말 것:

- field_validator 함수 수정
- 검증 로직 수정
- 필드 타입 수정
- 기존 기본값 수정
- 기존 제약 조건 수정
- 선택 필드에서 default= 생략
- 실제로 존재하지 않는 필드나 모델 추가

작업 후 POST /books의 선택 필드가 여전히 선택 사항인지 확인하세요.
전체 파일을 불필요하게 다시 출력하지 말고, 수정된 항목과 변경 이유를 보여 주세요.
```

---

## 4. 최종 누락 점검 및 검증

```text
main.py와 schemas.py를 수정하지 말고 최종 문서화 누락 여부만 점검해 주세요.

아래 항목을 표로 정리하세요.

1. tags 누락 엔드포인트
2. summary 누락 엔드포인트
3. 독스트링 누락 엔드포인트
4. 실제 raise HTTPException과 responses의 불일치
5. description 누락 필드
6. examples 누락 필드
7. 선택 필드의 default 누락 여부

추가로 다음을 확인하세요.

- /docs 응답 상태가 200인지
- /redoc 응답 상태가 200인지
- /openapi.json에 문서화 내용이 반영됐는지
- 태그가 없는 엔드포인트가 없는지
- responses에 실제로 존재하지 않는 오류 코드가 들어가지 않았는지
- 기존 API 응답과 상태 코드가 변경되지 않았는지

실제 코드에 없는 내용은 추측하지 말고, 각 지적사항에 파일명과 코드 근거를 함께 작성하세요.
코드는 수정하지 말고 검수 결과만 보고하세요.
```

---

## Postman 반영 방법

문서화 작업 후 Postman에 기존 컬렉션이 이미 있다면 최신 OpenAPI 문서를 다시 Import합니다.

1. 서버 실행

   ```powershell
   cd C:\Users\Playdata\Desktop\study\app-dev
   fastapi dev main.py
   ```

2. 브라우저에서 `http://127.0.0.1:8000/openapi.json` 접속
3. JSON 전체 복사
4. Postman의 Import 선택
5. Raw text에 붙여 넣고 Import
6. `baseUrl` 환경변수는 `http://127.0.0.1:8000`으로 설정

문서화는 기능을 바꾸지 않으므로, 기존 Postman 테스트가 모두 통과하는지도 확인합니다.
